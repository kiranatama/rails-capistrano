<%= form_for(@frooble) do |f| %>
  <% if @frooble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frooble.errors.count, "error") %> prohibited this frooble from being saved:</h2>

      <ul>
      <% @frooble.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
